name: Build our Code ( as of now )

on:
  push:
    branches: ["main"]

jobs:
  # name of the job
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - name: pulling our git repo 
        uses: actions/checkout@v6

      - name: say hi to myself(natique)
        run: echo "Natique Ibrar"



